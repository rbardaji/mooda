# Hobo._find_name(*header*)

From mooda >= 0.3.

It is a static method.

It looks for the label of the parameter in the header.

## Parameters

    header: str
        Header of one parameter.

## Returns

    name: str
        Label of the parameter.
